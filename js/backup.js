RSA.Backup=(()=>{async function render(){const d=await RSA.DB.exportData();backupInfo.textContent=`Máquinas: ${d.machines.length}\nPendências: ${d.pendencias.length}\nFotos: ${d.photos.length}\nHistórico: ${d.historico.length}\nÚltima leitura: ${RSA.nowBR()}`}
async function exportJson(){RSA.download(`rsa-frota-backup-${RSA.today()}.json`,JSON.stringify(await RSA.DB.exportData(),null,2));RSA.toast('Backup exportado.')}
async function importJson(file){if(!file)return;const data=JSON.parse(await file.text());if(!confirm('Importar este backup substituirá os dados atuais. Deseja continuar?'))return;await RSA.DB.importData(data);RSA.toast('Backup importado.');RSA.App.refresh()}
async function clearAll(){if(!confirm('Tem certeza que deseja limpar todos os dados?'))return;if(!confirm('Confirma novamente? Esta ação não pode ser desfeita.'))return;await RSA.DB.clear();RSA.toast('Todos os dados foram limpos.');RSA.App.refresh()}
async function seed(){const m1={id:RSA.uid(),placa:'ABC-1234',modelo:'Fiat Strada',gestor:'João da Silva',createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()},m2={id:RSA.uid(),placa:'XYZ-5678',modelo:'VW Saveiro',gestor:'Carlos Souza',createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()};await RSA.DB.put('machines',m1);await RSA.DB.put('machines',m2);await RSA.DB.put('pendencias',{id:RSA.uid(),machineId:m1.id,descricao:'Vazamento de óleo próximo ao motor.',dataRelato:RSA.today(),responsavel:'Checklist',prioridade:'Alta',situacao:'Aguardando execução',observacoes:'Exemplo opcional.',createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()});await RSA.DB.put('pendencias',{id:RSA.uid(),machineId:m2.id,descricao:'Problema no sistema de freios.',dataRelato:RSA.today(),responsavel:'Checklist',prioridade:'Crítica',situacao:'Em execução',numeroOS:'15432',numeroBoletim:'8451',previsaoConclusao:RSA.today(),createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()});RSA.toast('Exemplos carregados.');RSA.App.refresh()}
return{render,exportJson,importJson,clearAll,seed};})();
