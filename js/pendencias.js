RSA.Pendencias=(()=>{async function modal(machineId='',p={}){const ms=await RSA.DB.all('machines');if(!ms.length){RSA.toast('Cadastre uma máquina primeiro.');return}RSA.openModal(`<div class="modal-head"><div><h2>${p.id?'Editar':'Nova'} pendência</h2><p>Vincule o relato à máquina.</p></div><button class="btn btn-ghost" onclick="RSA.closeModal()">Fechar</button></div><form id="pendForm" class="form-grid"><input name="id" type="hidden" value="${p.id||''}"><input name="createdAt" type="hidden" value="${p.createdAt||''}"><div><label>Máquina *</label><select name="machineId" class="input">${ms.map(m=>`<option value="${m.id}" ${(p.machineId||machineId)===m.id?'selected':''}>${RSA.escape(m.placa)} - ${RSA.escape(m.modelo)}</option>`).join('')}</select></div><div><label>Data do relato *</label><input type="date" required name="dataRelato" class="input" value="${p.dataRelato||RSA.today()}"></div><div class="full"><label>Descrição *</label><textarea required name="descricao" class="input">${RSA.escape(p.descricao||'')}</textarea></div><div><label>Responsável pelo relato *</label><input required name="responsavel" class="input" value="${RSA.escape(p.responsavel||'')}"></div><div><label>Prioridade</label><select name="prioridade" class="input">${RSA.PRIORITIES.map(x=>`<option ${p.prioridade===x?'selected':''}>${x}</option>`).join('')}</select></div><div><label>Situação</label><select name="situacao" class="input">${RSA.STATUSES.map(x=>`<option ${p.situacao===x?'selected':''}>${x}</option>`).join('')}</select></div><div class="full"><label>Observações</label><textarea name="observacoes" class="input">${RSA.escape(p.observacoes||'')}</textarea></div><div class="full actions"><button class="btn btn-primary">Salvar pendência</button></div></form>`);pendForm.onsubmit=e=>{e.preventDefault();saveBasic(e.target,p).catch(err=>RSA.toast('Erro: '+err.message))}}
async function saveBasic(form,old={}){const id=form.id.value||RSA.uid(), now=new Date().toISOString();const obj={...old,id,machineId:form.machineId.value,descricao:form.descricao.value.trim(),dataRelato:form.dataRelato.value,responsavel:form.responsavel.value.trim(),prioridade:form.prioridade.value,situacao:form.situacao.value,observacoes:form.observacoes.value,createdAt:form.createdAt.value||now,updatedAt:now};await RSA.DB.put('pendencias',obj);await addHist(id,old.id?'Pendência atualizada':'Pendência cadastrada',obj.situacao,obj.responsavel);RSA.toast('Pendência salva.');RSA.closeModal();RSA.App.refresh();detail(id)}
async function detail(id){const p=await RSA.DB.get('pendencias',id), m=await RSA.DB.get('machines',p.machineId), photos=(await RSA.DB.all('photos')).filter(x=>x.pendenciaId===id), hist=(await RSA.DB.all('historico')).filter(x=>x.pendenciaId===id).sort((a,b)=>b.data.localeCompare(a.data));RSA.openModal(`<div class="modal-head"><div><h2>${RSA.escape(m.placa)} • Pendência</h2><p>${RSA.escape(p.descricao)}</p></div><button class="btn btn-ghost" onclick="RSA.closeModal()">Fechar</button></div><form id="detailForm" class="form-grid">${fields(p)}<div class="full actions"><button class="btn btn-primary">Salvar acompanhamento</button><button type="button" class="btn btn-danger" onclick="RSA.Pendencias.remove('${id}')">Excluir pendência</button></div></form><hr><h3>Fotos da pendência</h3><div class="form-grid"><input id="photoFile" type="file" accept="image/*" class="input"><input id="photoDesc" class="input" placeholder="Descrição da foto"><button class="btn btn-secondary" onclick="RSA.Pendencias.addPhoto('${id}')">Adicionar foto</button></div><div class="gallery">${photos.map(f=>`<div class="photo"><img src="${f.imagem}" onclick="RSA.Pendencias.zoom('${f.id}')"><small>${RSA.escape(f.descricao||'Sem descrição')}</small><button class="btn btn-sm btn-danger" onclick="RSA.Pendencias.delPhoto('${f.id}','${id}')">Excluir</button></div>`).join('')}</div><h3>Linha do tempo</h3><div class="form-grid"><input id="histEvento" class="input" placeholder="Evento"><input id="histDesc" class="input" placeholder="Descrição / usuário"><button class="btn btn-outline" onclick="RSA.Pendencias.manualHist('${id}')">Adicionar histórico</button></div><div class="timeline">${hist.map(h=>`<div class="timeline-item"><strong>${new Date(h.data).toLocaleString('pt-BR')} - ${RSA.escape(h.evento)}</strong><p>${RSA.escape(h.descricao||'')} ${h.usuario?'- '+RSA.escape(h.usuario):''}</p></div>`).join('')}</div>`);detailForm.onsubmit=e=>{e.preventDefault();saveDetail(id,new FormData(e.target),p).catch(err=>RSA.toast('Erro: '+err.message))}}
function fields(p){const inp=(n,l,t='text')=>`<div><label>${l}</label><input name="${n}" type="${t}" class="input" value="${RSA.escape(p[n]||'')}"></div>`;return `<div class="full"><label>Descrição</label><textarea name="descricao" class="input">${RSA.escape(p.descricao||'')}</textarea></div>${inp('dataRelato','Data','date')}${inp('responsavel','Responsável')}`+`<div><label>Prioridade</label><select name="prioridade" class="input">${RSA.PRIORITIES.map(x=>`<option ${p.prioridade===x?'selected':''}>${x}</option>`).join('')}</select></div><div><label>Situação</label><select name="situacao" class="input">${RSA.STATUSES.map(x=>`<option ${p.situacao===x?'selected':''}>${x}</option>`).join('')}</select></div><div class="full"><label>Observações</label><textarea name="observacoes" class="input">${RSA.escape(p.observacoes||'')}</textarea></div><h3 class="full">Ordem de Serviço e boletim</h3>${inp('numeroOS','Número da O.S.')}${inp('dataOS','Data de abertura da O.S.','date')}${inp('statusOS','Status da O.S.')}${inp('numeroBoletim','Número do boletim')}<h3 class="full">Execução por terceiro</h3><div><label>Será realizado por terceiro?</label><select name="terceiro" class="input"><option value="Não" ${p.terceiro!=='Sim'?'selected':''}>Não</option><option value="Sim" ${p.terceiro==='Sim'?'selected':''}>Sim</option></select></div>${inp('nomeTerceiro','Nome do terceiro')}${inp('empresaTerceiro','Empresa')}${inp('contatoTerceiro','Contato')}<div class="full"><label>Observações do terceiro</label><textarea name="obsTerceiro" class="input">${RSA.escape(p.obsTerceiro||'')}</textarea></div><h3 class="full">Previsão e execução</h3>${inp('previsaoExecucao','Previsão de execução','date')}${inp('previsaoConclusao','Previsão de conclusão','date')}<div class="full"><label>Serviço realizado</label><textarea name="servicoRealizado" class="input">${RSA.escape(p.servicoRealizado||'')}</textarea></div><div class="full"><label>Observações da execução</label><textarea name="obsExecucao" class="input">${RSA.escape(p.obsExecucao||'')}</textarea></div>${inp('dataConclusao','Data de conclusão','date')}${inp('responsavelExecucao','Responsável pela execução')}`}
async function saveDetail(id,fd,old){const obj={...old};for(const [k,v] of fd.entries())obj[k]=v;obj.updatedAt=new Date().toISOString();await RSA.DB.put('pendencias',obj);if(old.situacao!==obj.situacao)await addHist(id,'Situação alterada',`${old.situacao} → ${obj.situacao}`,obj.responsavel);else await addHist(id,'Acompanhamento atualizado','Dados detalhados salvos',obj.responsavel);RSA.toast('Acompanhamento salvo.');RSA.App.refresh();detail(id)}
async function addHist(pendenciaId,evento,descricao='',usuario=''){await RSA.DB.put('historico',{id:RSA.uid(),pendenciaId,data:new Date().toISOString(),evento,descricao,usuario})}
async function remove(id,silent=false){if(!silent&&!confirm('Excluir esta pendência e todos os anexos/histórico?'))return;for(const f of (await RSA.DB.all('photos')).filter(x=>x.pendenciaId===id))await RSA.DB.del('photos',f.id);for(const h of (await RSA.DB.all('historico')).filter(x=>x.pendenciaId===id))await RSA.DB.del('historico',h.id);await RSA.DB.del('pendencias',id);if(!silent){RSA.toast('Pendência excluída.');RSA.closeModal();RSA.App.refresh()}}
async function addPhoto(id){if(!photoFile.files[0])return RSA.toast('Selecione uma foto.');await RSA.DB.put('photos',{id:RSA.uid(),pendenciaId:id,imagem:await RSA.fileToDataURL(photoFile.files[0]),descricao:photoDesc.value,createdAt:new Date().toISOString()});await addHist(id,'Foto adicionada',photoDesc.value);RSA.toast('Foto salva.');detail(id)}
async function delPhoto(fid,pid){if(confirm('Excluir foto?')){await RSA.DB.del('photos',fid);await addHist(pid,'Foto excluída');detail(pid)}}
async function zoom(fid){const f=await RSA.DB.get('photos',fid);RSA.openModal(`<div class="modal-head"><h2>${RSA.escape(f.descricao||'Foto')}</h2><button class="btn btn-ghost" onclick="RSA.closeModal()">Fechar</button></div><img src="${f.imagem}" style="max-width:100%;border-radius:16px">`)}
async function manualHist(id){if(!histEvento.value.trim())return RSA.toast('Informe o evento.');await addHist(id,histEvento.value,histDesc.value);detail(id)}
async function edit(id){const p=await RSA.DB.get('pendencias',id);modal(p.machineId,p)}
return{modal,edit,detail,remove,addPhoto,delPhoto,zoom,manualHist};})();
